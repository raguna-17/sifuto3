[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

addopts =
    --maxfail=1
    -vv
    --cov=app
    --cov-report=term-missing
    --disable-warnings
    --tb=short